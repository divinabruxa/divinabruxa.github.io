const items=[['Hoje','Carta do Dia + Pulso da Orbe','Um encontro breve para começar.'],['Esta semana','Tiragem temática','Uma pergunta, uma estrutura, uma nova perspectiva.'],['Próximo episódio','De Frente com o Tarot','Vídeo reservado para sua jornada de estudo.'],['Próxima faixa','Música & Vibração','Som ambiente para acompanhar seu ritual.']];
export class EcosystemEngine{constructor(root){this.root=root;this.render();}render(){if(!this.root)return;const host=document.createElement('aside');host.className='ecosystem-v6';host.setAttribute('aria-label','Agenda editorial');host.innerHTML=`<div><p class="eyebrow">AGENDA DA ORBE</p><h3>Sempre existe um próximo encontro.</h3><p>Conteúdo editorial em ciclos, sem notificações invasivas.</p></div><div class="ecosystem-list">${items.map(([when,title,desc])=>`<article><small>${when}</small><strong>${title}</strong><span>${desc}</span></article>`).join('')}</div>`;this.root.prepend(host);}}
