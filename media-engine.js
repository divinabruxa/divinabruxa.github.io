export class MediaEngine{
  constructor(roots,config){this.roots=roots;this.config=config;this.renderVideos();this.renderMusic();}
  renderVideos(){const videos=this.config.youtubeVideos||[];this.roots.videos.innerHTML=`<article class="media-hero video-hero"><div class="media-mark">▶</div><div><p class="eyebrow">DE FRENTE COM O TAROT</p><h3>Um capítulo novo a cada semana.</h3><p>Todos os episódios ficam organizados em páginas numeradas, formando uma biblioteca permanente.</p><a class="primary" href="${this.config.youtube}" target="_blank" rel="noopener">Abrir canal no YouTube</a></div></article><div class="chapter-head"><h3>Capítulos</h3><span>${videos.length} publicados</span></div><div class="episode-grid">${videos.length?videos.map((v,i)=>`<article><span>${String(i+1).padStart(2,'0')}</span><h3>${v.title}</h3><p>${v.description||'Novo encontro com os símbolos do Tarot.'}</p><a href="${v.url}" target="_blank" rel="noopener">Assistir capítulo</a></article>`).join(''):'<article><span>01</span><h3>O primeiro capítulo está a caminho</h3><p>Quando seu primeiro vídeo estiver publicado, adicione título e link em <b>youtubeVideos</b> no arquivo <b>config.js</b>.</p></article>'}</div>`;}
  renderMusic(){const albums=this.config.spotifyAlbums||[];this.roots.music.innerHTML=`<article class="media-hero album-hero"><div class="album-art"><span>HÉRCULES DX</span><b>♫</b><small>DOIS ÁLBUNS</small></div><div><p class="eyebrow">MÚSICA AUTORAL</p><h3>Sobre as Estrelas & Z</h3><p>Escolha um álbum e deixe a música acompanhar sua passagem pelas camadas da Orbe.</p></div></article><div class="album-library">${albums.map(a=>`<article><p class="eyebrow">${a.artist}</p><h3>${a.name}</h3><div class="spotify-frame"><iframe title="${a.name} no Spotify" src="https://open.spotify.com/embed/album/${a.id}?utm_source=generator&theme=0" width="100%" height="352" frameborder="0" allowfullscreen="" allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture" loading="lazy"></iframe></div><a class="text-button" href="https://open.spotify.com/album/${a.id}" target="_blank" rel="noopener">Abrir no Spotify</a></article>`).join('')}</div>`;}
}
