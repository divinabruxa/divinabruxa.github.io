import { SKINS_V6 } from './skin-catalog-v6.js';
export class SkinsEngine{constructor(root){this.root=root;this.render();}render(){if(!this.root)return;this.root.innerHTML=`<div class="skins-v6-grid">${SKINS_V6.map(s=>`<article class="skin-tile skin-${s.id}" data-skin="${s.id}"><div class="skin-preview"><span>✦</span></div><div><h3>${s.name}</h3><p>${s.status==='active'?'Ativa':'Em criação individual'}</p><strong>${s.priceCents?'R$ '+(s.priceCents/100).toFixed(2).replace('.',','):'Grátis'}</strong></div><button type="button" ${s.status==='active'?'':'disabled'}>${s.status==='active'?'Ativar':'Em breve'}</button></article>`).join('')}</div>`;}}
