/* DIVINA BRUXA — ADMIN CÓSMICO V8 */
const MODULES=['Hoje','Financeiro','Usuários','Premium e IA','Tarot','Escola','Conteúdo','Consultas','Loja','Música e Vídeos','Analytics','Segurança'];
const render=()=>{const root=document.querySelector('#admin');if(!root||root.querySelector('[data-admin-v8]'))return;const panel=document.createElement('aside');panel.dataset.adminV8='';panel.className='admin-v8-guide';panel.innerHTML=`<span class="admin-v8-sigil" aria-hidden="true">✦</span><div><p class="eyebrow">CENTRO DE CONTROLE</p><h3>Administração da Divina Bruxa</h3><p>O painel seguro reunirá operação, conteúdo, receita e saúde do universo sem exibir o corpo privado dos Diários.</p><div class="admin-v8-modules">${MODULES.map(item=>`<span>${item}</span>`).join('')}</div><small>Acesso real exige autenticação da proprietária e MFA no backend seguro.</small></div>`;const form=root.querySelector('#adminLogin');root.insertBefore(panel,form||root.firstChild)};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',render,{once:true});else render();
