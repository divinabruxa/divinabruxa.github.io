/* DIVINA BRUXA — ACESSIBILIDADE E ESTABILIDADE V8 */
const enhance=()=>{document.querySelectorAll('button:not([type])').forEach(button=>{if(!button.closest('form'))button.type='button'});document.querySelectorAll('img:not([alt])').forEach(img=>img.alt='');document.querySelectorAll('.screen[id]').forEach(screen=>{screen.setAttribute('role','region');const heading=screen.querySelector('h1,h2,h3');if(heading&&!heading.id)heading.id=`${screen.id}-title`;if(heading)screen.setAttribute('aria-labelledby',heading.id)});document.querySelectorAll('[data-go]').forEach(node=>{if(!node.getAttribute('aria-label')){const label=node.textContent.trim().replace(/\s+/g,' ');if(label)node.setAttribute('aria-label',label.slice(0,90))}})};
const boot=()=>{enhance();new MutationObserver(enhance).observe(document.body,{childList:true,subtree:true});document.addEventListener('keydown',event=>{if(event.key==='Escape')document.querySelector('dialog[open]')?.close?.()})};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
