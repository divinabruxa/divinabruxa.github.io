import { store } from './storage.js';import { ANALYTICS_POLICY } from './analytics-policy.js';
const KEY='analytics-events-v5';const read=()=>store.get(KEY,[]);const write=e=>store.set(KEY,e.slice(-100));
export class AnalyticsEngine{constructor(root){this.root=root;document.addEventListener('click',e=>{const target=e.target.closest('[data-go]');if(target)this.track('feature_open',target.dataset.go);});if(root)this.render();}track(type,value){if(!ANALYTICS_POLICY.collects.includes(type)||ANALYTICS_POLICY.neverCollects.includes(value))return;write([...read(),{type,value:String(value).slice(0,40),at:new Date().toISOString()}]);}render(){const events=read(),counts=events.reduce((a,e)=>(a[e.value]=(a[e.value]||0)+1,a),{});this.root.innerHTML=`<div class="analytics-v5"><section class="analytics-hero"><div><p class="eyebrow">ANALYTICS · PRIVACIDADE</p><h3>Ver o movimento sem invadir ninguém.</h3><p>Esta versão mede apenas eventos agregados do aparelho. Nenhum texto privado, contato ou pergunta é coletado.</p></div><span>◈</span></section><div class="analytics-cards"><article><small>Eventos locais</small><strong>${events.length}</strong></article><article><small>Retenção</small><strong>${ANALYTICS_POLICY.retentionDays}d</strong></article><article><small>Provedor externo</small><strong>Não</strong></article></div><section class="analytics-panel"><div class="analytics-head"><h3>Áreas mais visitadas</h3><small>somente este aparelho</small></div><div class="analytics-bars">${Object.entries(counts).sort((a,b)=>b[1]-a[1]).slice(0,8).map(([name,count])=>`<div><span>${name}</span><i><b style="width:${Math.min(100,count*14)}%"></b></i><em>${count}</em></div>`).join('')||'<p>Ainda não há eventos registrados.</p>'}</div></section><button type="button" class="analytics-clear" data-clear-analytics>Apagar métricas deste aparelho</button></div>`;this.root.querySelector('[data-clear-analytics]')?.addEventListener('click',()=>{store.remove(KEY);this.render();});}}
